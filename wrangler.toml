# Cloudflare Pages configuration (used by `npx wrangler pages deploy`
# and by the Pages dashboard when connected to Git).
name = "adsolution"
compatibility_date = "2026-09-01"
pages_build_output_dir = "out"
